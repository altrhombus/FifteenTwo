/// Lightweight physical-board companion scorekeeper: pegs, matches, skunk rules.
/// Deliberately independent of CribbageKit's card model — no cards, decks, or crib here.
public enum CribbageBoardKit {
    public static let version = "0.0.1"
}
