/// Pure-Swift cribbage rules engine: cards, scoring, solvers, RNG, and game state.
/// No platform imports — everything here must be testable with `swift test` alone.
public enum CribbageKit {
    public static let version = "0.0.1"
}
