/// Camera-based hand scanning: rectangle detection, perspective correction, rank OCR,
/// and a small custom suit classifier. Later phase — see docs/plan.md.
public enum CribbageVision {
    public static let version = "0.0.1"
}
