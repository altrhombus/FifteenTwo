/// SwiftData persistence: match history, spaced-repetition drill stats, and settings.
/// See `CribbageDataStack` for the shared `ModelContainer` factory and
/// `BoardHistoryStore` for the save/fetch/delete API physical-board mode uses.
public enum CribbageData {
    public static let version = "0.0.1"
}
